# Copilot Instructions

## Git Operations

Never run `git commit`, `git push`, or any other Git write operation without **explicit user approval**. Always describe what you intend to commit/push and wait for confirmation before executing.
